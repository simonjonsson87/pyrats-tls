name: Build Wheels

on:
  push:
    branches: ["*"]
  pull_request:
  workflow_dispatch:

jobs:
  build_wheels:
    name: Build wheels on ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install cibuildwheel
        run: pip install cibuildwheel

      - name: Build wheels
        run: |
          cibuildwheel --output-dir wheelhouse
        env:
          CIBW_SKIP: "pp* cp36* cp37*"
          CIBW_BUILD: "cp38-* cp39-* cp310-* cp311-*"
          CIBW_ENVIRONMENT_LINUX: >
            CMAKE_ARGS="-CMAKE_BUILD_TYPE=Release"
          CIBW_ENVIRONMENT_MACOS: >
            CMAKE_ARGS="-CMAKE_BUILD_TYPE=Release"
          CIBW_ENVIRONMENT_WINDOWS: >
            CMAKE_ARGS="-CMAKE_BUILD_TYPE=Release -CMAKE_PREFIX_PATH=C:/rats-tls"
          CIBW_BEFORE_BUILD_LINUX: |
            apt-get update && apt-get install -y git make autoconf libtool gcc g++ libssl-dev libcbor-dev
            git clone https://github.com/inclavare-containers/rats-tls.git
            cd rats-tls
            cmake -B build -CMAKE_INSTALL_PREFIX=/usr/local -DTLS_TYPE=openssl -DWITH_SGX=OFF -DWITH_SEV=ON
            cmake --build build --target install
            cd ..
            pip install cmake ninja
          CIBW_BEFORE_BUILD_MACOS: |
            brew install openssl@3 cbor
            git clone https://github.com/inclavare-containers/rats-tls.git
            cd rats-tls
            cmake -B build -CMAKE_INSTALL_PREFIX=/usr/local -DTLS_TYPE=openssl -DWITH_SGX=OFF
            cmake --build build --target install
            cd ..
            pip install cmake ninja
          CIBW_BEFORE_BUILD_WINDOWS: |
            choco install git make openssl
            git clone https://github.com/inclavare-containers/rats-tls.git
            cd rats-tls
            cmake -G "Visual Studio 16 2019" -A x64 -B build -CMAKE_INSTALL_PREFIX=C:/rats-tls -DTLS_TYPE=openssl -DWITH_SGX=OFF
            cmake --build build --config Release --target install
            cd ..
            pip install cmake ninja

      - name: Upload wheels
        uses: actions/upload-artifact@v4
        with:
          name: wheels-${{ matrix.os }}
          path: wheelhouse/*.whl

      - name: Test wheels
        run: |
          pip install pytest
          pip install pyrats_tls --no-index --find-links=wheelhouse
          pytest tests